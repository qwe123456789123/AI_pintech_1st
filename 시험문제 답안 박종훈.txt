1.   
SELECT * FROM USER_CONSTRAINTS WHERE TABLE_NAME='MEMBER';
SELECT * FROM USER_CONS_COLUMNS WHERE TABLE_NAME='MEMBER';

2. 
SELECT E.*,D.DNAME FROM EMP E, DEPT D WHERE E.DEPTNO = D.DEPTNO 
SELECT E.*,D.DNAME FROM EMP E JOIN DEPT D ON E.EMPNO = D.DEPTNO

3. ALTER TABLE MEMBER ADD MOBILE VARCHAR(15) NOT NULL;

4. 1)트랜잭션  2)DCL  3) 섹션 4)COMMIT 5)ROLLBACK 6) 무결성

강사님 사랑해요